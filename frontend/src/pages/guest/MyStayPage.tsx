import React from 'react';

export default function MyStayPage({ onNavigate }: { onNavigate: (path: string) => void }) {
  const booking = JSON.parse(localStorage.getItem('meridian_latest_booking') || 'null');
  return <main className="my-stay-page"><button className="my-stay-back" onClick={() => onNavigate('/guest')}>← Meridian Resorts &amp; Spas</button><div className="my-stay-inner"><p className="guest-auth-eyebrow">Your private itinerary</p><h1>My Stay</h1>{booking ? <div className="my-stay-card"><span className="confirmation-id">{booking.id}</span><h2>{booking.property}</h2><p>{booking.location}</p><div className="my-stay-grid"><div><span>Stay dates</span><strong>{booking.checkIn} → {booking.checkOut}</strong></div><div><span>Room</span><strong>{booking.room.name}</strong></div><div><span>Spa</span><strong>{booking.spa?.name || 'No experience selected'}</strong></div><div><span>Preferences</span><strong>{booking.preferences?.length ? booking.preferences.join(', ') : 'No preferences recorded'}</strong></div></div></div> : <div className="my-stay-empty"><h2>Your next chapter is waiting.</h2><p>Begin planning a Meridian stay and your itinerary will appear here.</p><button onClick={() => onNavigate('/booking')}>Plan Your Stay</button></div>}<button className="booking-secondary" onClick={() => onNavigate('/guest')}>Explore the resort</button></div></main>;
}
